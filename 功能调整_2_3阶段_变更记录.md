# 功能调整 第二 / 第三阶段 —— 变更记录

> 记录时间：2026-08-29
> 范围：`new-chat-7/health-archive/`（Flutter 工程）
> 原则：只调整功能归属与数据模型，**未做 UI 重构**（颜色 / 字体 / 圆角 / 间距 / 图标 / 动画均未动）。
> 第一阶段（人物上下文 / currentProfileId / 身体·记录共用数据源）此前已确认，无需改动。

---

## ⚠️ 未完成 / 需要在开发机补做（重点看这里）

| # | 事项 | 阶段 | 为什么没做 | 补做方式 |
|---|---|---|---|---|
| 1 | 用药「用法」字段（口服 / 外用 / 饭前等） | 二 §10 | `medications` 表加列 → schema 迁移 → 必须重跑 `build_runner` 重新生成 `app_database.g.dart`。本机无 flutter/dart，手改生成代码风险高。 | 表加 `TextColumn get usage => text().nullable()()`；`schemaVersion` 9→10；迁移 `if (from < 10) await m.addColumn(medications, medications.usage)`；`dart run build_runner build`；用药编辑页加一个输入框。其余用药字段（名称/剂量/单位/频率/起止/备注）已具备。 |
| 2 | `MetricValue` 的通用 `source_id` 列 | 三 §6 §7 | 同样需要 schema 迁移 + 代码生成；且 §14 明确「现在不用立即接 Apple Health」。 | 真正接 Apple Health / 设备导入的那次迭代里，给 `health_metrics` 加可空 `source_id`（HealthKit 样本 UUID / 设备 id）。**报告来源现在用 `reportId` 承担 sourceId 职责**，够用。已在 `app_database.dart` 该列上写了注释说明。 |
| 3 | 未能运行 `flutter analyze` / `flutter test` | 二 三 | 运行环境 PATH 上没有 flutter / dart。 | 在开发机跑一遍静态检查 + 现有测试套件（含本次新增的 `test/metric_source_test.dart`）。以下所有改动均为**静态阅读核对**，未经编译验证。 |

---

## 🔶 较大变化（需要你确认方向）

| 变化 | 文件 | 说明 |
|---|---|---|
| **首页铃铛的目标页换了** | `home_page.dart` / 新增 `notification_center_page.dart` | 原来铃铛进入 `RemindersPage`（提醒管理 + 最近通知混在一页）。现在铃铛进入新的**「通知中心」**，只显示实际产生的通知 / 提醒历史 + 全部已读。提醒的**管理**（新建复查提醒、服药计划、开关、时间）保留在「我的 → 提醒」。对应 §2-15「通知 / 提醒 / 用药记录三个概念分开」。 |
| **`RemindersPage` 删掉「最近通知」段** | `reminders_page.dart` | 该页现在是纯提醒管理页。通知历史移到通知中心。 |
| **记录页新增「用药记录」入口卡片** | `records_page.dart` | 记录页顶部（引导语下方）加了一张 `ListTile` 卡片跳到 `MedicationPage`。这是一处**可见的新增元素**（非重构，但你会看到多一张卡）。对应 §2-4 / §2-11。 |
| **「今日一则」替换「健康冷知识」** | `health_tip_card.dart` 等 | 纯文案：标题「健康冷知识」→「今日一则」，按钮「换一条」→「换一则」。逻辑不变（一次一条、可换、无 Feed）。对应 §2-3。 |

---

## 第二阶段 —— 已完成改动

| 条款 | 改动 | 文件 |
|---|---|---|
| §3 今日一则 | 文案 健康冷知识→今日一则、换一条→换一则；补注释「不做 Feed / 列表 / 无限下滑」 | `lib/widgets/health_tip_card.dart`、`lib/data/health_tips.dart` |
| §15 通知/提醒分离 | 新增「通知中心」页；首页铃铛指向它；tooltip 提醒→通知 | 新增 `lib/pages/notification_center_page.dart`、改 `lib/pages/home_page.dart` |
| §15 通知/提醒分离 | `RemindersPage` 去掉「最近通知」段与「全部已读」按钮，只留提醒管理；更新类注释 | `lib/pages/reminders_page.dart` |
| §4 / §11 用药进记录 | 记录页加「用药记录」入口卡片 → 同一个 `MedicationPage`（同一套数据，不是第二套） | `lib/pages/records_page.dart` |
| 测试跟随 | `widget_test.dart` 断言 健康冷知识→今日一则；`compile_check_test.dart` 收录 `NotificationCenterPage` | `test/widget_test.dart`、`test/compile_check_test.dart` |

### 第二阶段 —— 核对后未改动（已符合）

- §1 首页轻量：body 只有「今日一则」；无评分 / 异常总览 / 血糖血压体重 / 报告列表 / AI / 资讯 Feed。
- §2 首页无报告搜索：`home_page.dart` 早已无搜索入口，搜索只在记录页。
- §5 记录页搜索范围：`records_filter.dart` 的 `matchesReport` 已把 `metricNames` + `rawText` 纳入关键词匹配，搜「尿酸」能命中含该指标的报告（`report_tags_test` / `records_filter_test` 覆盖）。
- §6 添加页入口：`add_page.dart` 已含 拍摄 / 上传化验单 / 影像·病理 / 手工录入 / 日常记录 / **用药** 六个入口。
- §7 化验单流程：拍摄·上传 → OCR → 识别 → 标准化 → 存原件 → 写记录 → 身体页从同一份 `health_metrics` 推导。
- §8 影像/病理：`imaging_report_page.dart` 存医院 / 日期 / 类型 / 正文 + 原图·PDF，未套用化验指标模型。
- §9 日常记录：体重 / 血压 / 血糖 / 心率，保存后记录视角 + 身体视角（指标历史趋势）。
- §10 用药字段：名称 / 剂量 / 单位 / 频率（每日次数）/ 开始 / 结束 / 备注 已具备（缺「用法」，见未完成 #1）。
- §11 数据层：`medications.profileId` + `insertMedication` 默认当前档案；「我的 → 用药记录」按 §11 允许保留为兼容入口，跳同一页。
- §12 疾病史：`getAllDiseases` / `insertDisease` 按 `_activeProfileId` 过滤 —— 已绑定 currentProfileId。入口按 §12 暂留「我的」。
- §13 我的职责：本次未往「我的」加任何健康数据入口。
- §14 家庭成员：「我的 → 家庭成员」做增删改；首页顶部切换器做快速切换。两者含义不同、不冲突。

---

## 第三阶段 —— 已完成改动

| 条款 | 改动 | 文件 |
|---|---|---|
| §7 §14 §15 来源体系收敛 | **新增** `MetricSourceKind` 枚举 + wire 值映射 + 中文标签 + 记录页可见筛选项列表。把散落的来源字符串常量收敛到一处，并**预留** `appleHealth` / `device` / `importedFile` 三种未来来源（未实现，只占位）。 | 新增 `lib/models/metric_source.dart` |
| §7 §9 接线 | 记录页 `sourceTypeLabel` 改为委托新模块；`RealEntry.source` 标签走同一套（文案不变）；来源筛选 chip 处加注释指向 `visibleRecordSourceFilters` 扩展位。 | `lib/pages/records_page.dart` |
| §6 §7 文档化 | `health_metrics.sourceType` 列加注释：规范值 + 未来 `source_id` 列的落位计划（不在本阶段做迁移）。 | `lib/data/app_database.dart` |
| 测试 | **新增** `test/metric_source_test.dart`（归一化 / wire 往返 / 标签 / 可见筛选项）；`compile_check_test.dart` 收录新模块 + 4 条断言。 | `test/metric_source_test.dart`、`test/compile_check_test.dart` |

### 第三阶段 —— 核对后未改动（已符合，无需动）

- §1 身体页分类：`coreBodyAreaOrder` 已是「系统 / 器官 / 领域」混合视角（肾脏·泌尿 / 心血管 / 肝胆 / 代谢 / 甲状腺 / 血液 / 电解质 …），未强行改成具体器官。
- §2 需关注排序但不只显示异常：`buildBodyAreaHealthFromMetrics(includeEmptyCoreAreas: true)` + 身体页「正常 / 数据不足」可折叠展开（`NormalItemsToggle`）—— 所有系统 / 指标 / 正常项 / 数据不足项都看得到。
- §3 状态语言克制：`computeStatus` → 正常 / 偏高 / 偏低 / 未判断；`normalizedBodyAreaStatus` → 异常 / 需关注 / 正常 / 数据不足；身体详情页有「不等同于医学诊断」免责说明。未把「超出参考范围」等同于诊断。
- §4 标准 Metric 库：`lib/models/metric_dictionary.dart` 已有 `MetricDefinition`（metricId / metricName=canonicalName / unit / bodySystem / aliases / typicalRange）+ `matchMetric` 别名归一。「空腹血糖 / FPG / FBG / 空腹葡萄糖」等归到同一 `FPG`。
- §5 手工录入优先标准指标：`manual_metric_entry_page.dart` 必须先从字典 `showMetricSelector` 选指标；「字典里没有，新增自定义指标」是次级动作，不是默认，生成 `CUSTOM_<时间戳>` id。
- §6 MetricValue 结构：`health_metrics` 每行即一次具体数值，已含 id / profileId / metricId / value / unit / referenceMin / referenceMax / measuredAt / sourceType / reportId / createdAt。仅缺通用 `sourceId`（见未完成 #2）。
- §8 来源差异不被抹掉：每行独立保留自己的 `sourceType` + `reportId`；指标历史页按行显示「来源报告 #id」；身体 / 记录页从不把多来源合成一条无来源趋势。
- §10 报告「影响部位」自动生成：`affectedBodyAreasForMetrics()` 由指标的 `bodySystem` 自动映射身体系统，记录页报告卡片与报告详情页都用它，**没有任何手动维护入口**。
- §11 报告类型分模型：化验报告产出 `health_metrics`（指标 / 数值 / 单位 / 参考范围）；影像·病理走 `imaging_report_page` 只存 `rawText` 描述性文本，不强产指标。两条路径已分开。
- §12 §13 实体关系：`PersonProfiles` / `MedicalReports`（含 `reportId` 关联 `health_metrics`）/ `HealthMetrics` / `Medications` / `Reminders` / `metric_dictionary` 已对应建议模型。
- §16 无健康评分：全仓无总健康评分 / 器官评分 / 风险分 / AI 打分代码。
- §17 无底层 AI 判定：状态判定全部本地按参考范围计算（`computeStatus`），不信任模型输出。

---

## 本次改动文件清单

### 新增
- `lib/pages/notification_center_page.dart`（二 §15）
- `lib/models/metric_source.dart`（三 §7 §14 §15）
- `test/metric_source_test.dart`

### 修改
- `lib/widgets/health_tip_card.dart`、`lib/data/health_tips.dart`（二 §3）
- `lib/pages/home_page.dart`（二 §15）
- `lib/pages/reminders_page.dart`（二 §15）
- `lib/pages/records_page.dart`（二 §4/§11、三 §7/§9）
- `lib/data/app_database.dart`（三 §6/§7，**仅注释**，无 schema 变更）
- `test/widget_test.dart`、`test/compile_check_test.dart`

### 未改（数据库 schema）
- `schemaVersion` 仍为 9。本次两阶段**没有任何数据库迁移**。用药「用法」列与 `source_id` 列都留给开发机（见上）。

---

## 停止点

第三阶段到此为止。未开始：UI 重构 / 视觉升级 / 动画 / 字体系统 / 颜色系统 / AI 大功能。
等功能结构与数据结构稳定后，再单独做完整 UI/UX 设计。
