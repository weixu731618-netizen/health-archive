# 健康 App 三页面结构优化 + iOS 原生交互调整执行任务

## 任务背景

当前 App 已经完成三个核心页面：

- 首页
- 身体
- 记录

当前整体方向已经基本正确，不需要推翻 UI，不需要重做视觉系统。

本次任务是在现有基础上进行一次“信息架构 + 交互结构 + 状态逻辑”优化，重点解决：

1. 首页“需要关注”中健康状态、复查任务、慢病信息混在一起
2. 身体页一级分类混用了器官、系统、代谢、指标组
3. “异常”与“等待复查”状态混淆
4. 记录页一级筛选和二级筛选层级不清
5. 底部悬浮 `+` 与首页三个新增入口重复
6. 当前底部大 `+` 不够符合原生 iOS 交互习惯
7. 慢病能力需要继续保留，但保持“无痕化”
8. 保持报告为核心入口，避免产品重新变成慢病管理 App

---

# 一、最高优先级产品原则

整个项目后续继续遵守：

```text
报告是入口

首页看当前
身体看部位
记录看历史

器官 / 系统是主要健康导航
指标属于器官 / 系统内部

异常形成关注
复查形成闭环

慢病是长期关系
不是全局产品模式

原始资料始终可查看
用户数据最终可导出
```

---

# 二、本次不要做的事情

本次禁止：

```text
重做整个 UI
修改主题颜色
大规模换 icon
重做字体
重写导航框架
新增慢病 Tab
新增 AI Tab
新增提醒 Tab
新增指标 Tab
新增健康商城
新增社区
新增资讯
大改 OCR
重建整个数据库
顺手重构无关页面
```

目标是：

```text
局部修正结构
保持现有视觉
降低产品复杂度
```

---

# 三、底部导航重新确定

当前底部：

```text
首页
身体
记录
+
```

其中中央存在一个大型悬浮 `+`。

本次修改后：

```text
首页
身体
记录
```

只保留三个真正的 Tab。

删除当前底部中央大型悬浮 `+`。

---

# 四、为什么删除底部大型 `+`

原因：

```text
首页已经存在三个核心新增入口
底部 + 与首页入口高度重复
+ 当前视觉权重过高
+ 会遮挡内容
+ 会抢页面主要信息注意力
+ 更像 Android/FAB 或高度自定义控件
+ 不符合当前追求的克制原生 iOS 风格
```

Tab Bar 只负责：

```text
页面切换
```

不要再承担：

```text
新增动作
```

---

# 五、新的“新增”交互原则

统一改成：

```text
首页：
直接使用首页三个核心新增入口

身体一级：
默认不放全局 +

器官 / 系统详情：
右上角允许出现 +

记录：
右上角放 +
```

---

# 六、首页新增入口

首页继续保留当前三个入口：

```text
拍报告
相册 / 文件
添加影像
```

其中：

```text
拍报告
```

继续作为视觉第一核心动作。

推荐层级：

```text
一级：
拍报告

二级：
相册 / 文件
添加影像
```

不要再加额外 `+`。

---

# 七、首页三个入口的产品含义

这三个入口属于：

```text
核心健康资料导入快捷入口
```

不是完整“添加菜单”。

首页只展示最重要、最高频、最符合产品定位的三个动作。

不要把所有辅助添加能力都塞到首页。

---

# 八、记录页新增入口

记录页右上角增加：

```text
+
```

示例：

```text
记录                     搜索   +
```

点击 `+` 后打开 iOS 风格 Menu / Action Menu。

---

# 九、记录页 + 菜单内容

`+` 代表：

```text
完整新增菜单
```

包括：

```text
拍报告
相册 / 文件
添加影像
手动记录
```

以及当前项目中已有的另外三个辅助添加功能。

保留现有全部 6 个左右的新增能力。

---

# 十、+ 菜单必须与首页三个入口形成职责区别

首页：

```text
核心高频资料导入
```

记录页 `+`：

```text
完整新增能力
```

因此允许功能部分重复。

但是不能让两者体验完全一样。

首页只展示：

```text
拍报告
相册 / 文件
添加影像
```

完整 `+` 菜单展示：

```text
拍报告
相册 / 文件
添加影像
手动记录
辅助功能 A
辅助功能 B
辅助功能 C
```

具体辅助功能名称使用当前项目已有功能。

---

# 十一、身体一级页新增入口

身体一级页默认不显示 `+`。

当前页面核心职责是：

```text
查看各器官 / 系统状态
```

不要让新增入口抢占注意力。

---

# 十二、器官 / 系统详情页允许上下文新增

例如用户进入：

```text
肾脏 / 泌尿
```

右上角可以显示：

```text
+
```

点击后：

```text
添加当前系统相关资料
```

菜单可包含：

```text
拍报告
相册 / 文件
添加影像
手动记录
```

如果技术结构允许：

上传后默认带入：

```text
currentOrganId / currentSystemId
```

作为“建议关联”。

不要强制绑定，用户仍可修改。

---

# 十三、首页整体结构保持

首页继续保持当前结构：

```text
首页

通知
个人中心

拍报告
拍照识别并保存健康资料

相册 / 文件
添加影像

需要关注

最近 / 当前事项（如果已有）
```

---

# 十四、首页“需要关注”问题

当前存在：

```text
代谢
2项异常 · 持续关注

复查眼底检查（2型糖尿病）
```

这里混合了：

```text
HealthAttention
FollowUpTask
ChronicCondition
```

需要拆开逻辑。

---

# 十五、健康状态类展示

例如：

```text
代谢
2项异常 · 持续关注
```

属于：

```text
HealthAttention
```

表达：

```text
身体哪里需要关注
```

可以继续显示。

---

# 十六、复查任务类展示

当前：

```text
复查眼底检查（2型糖尿病）
```

修改为：

```text
眼底检查
复查已到期
```

或：

```text
眼底检查
距离复查还有 12 天
```

不要在首页标题直接高频显示疾病名称。

---

# 十七、慢病信息继续无痕化

首页不优先展示：

```text
糖尿病
高血压
高血脂
```

首页优先展示：

```text
需要关注什么
需要做什么
```

具体慢病关联在详情页显示：

```text
关联长期关注：
2型糖尿病
```

---

# 十八、需要关注排序

首页“需要关注”排序：

```text
1. 已到期复查
2. 即将到期复查
3. 当前明确异常
4. 长期关注
```

---

# 十九、通知 Badge 优化

不要把所有普通通知都累计成高警示红色 badge。

区分：

```text
重要待处理
普通通知
OCR完成
资料归档
系统消息
```

只有真正需要用户处理的事项建议进入红色 badge，例如：

```text
复查已到期
OCR识别失败
需要用户确认报告
上传失败
```

普通“识别完成”不建议全部计入红色 badge。

---

# 二十、身体页顶部统计保留

当前：

```text
1 有近期记录
4 需要关注
8 暂无记录
```

保留。

不要恢复：

```text
健康档案完成度 45%
```

---

# 二十一、身体页当前分类问题

当前一级存在：

```text
代谢
肾脏 / 泌尿
心血管
电解质
```

这些不是同一层级。

因为：

```text
肾脏 / 泌尿 = 器官/系统
心血管 = 身体系统
代谢 = 生理功能领域
电解质 = 指标分类
```

必须统一。

---

# 二十二、身体一级只保留“器官 / 身体系统”

一级建议统一为：

```text
心脑血管
呼吸系统
消化系统
肝胆
胰腺
肾脏 / 泌尿
内分泌 / 代谢
血液系统
眼睛
耳鼻喉
口腔
骨骼 / 关节
皮肤
生殖系统
其他
```

具体名称可根据现有项目数据适配。

核心原则：

```text
一级必须全部属于器官 / 身体系统
```

---

# 二十三、指标组全部下沉

以下内容不要继续作为一级：

```text
电解质
血糖
血脂
尿酸
肝功能
肾功能
炎症
营养
肿瘤标志物
```

这些改成：

```text
器官 / 系统内部指标组
```

---

# 二十四、异常与复查必须分离

必须实现：

```text
异常 != 等待复查
```

---

# 二十五、身体状态体系

统一：

```text
NO_RECORD
暂无记录

RECENT_RECORD
近期有记录

ATTENTION
需要关注

FOLLOW_UP
等待复查

LONG_TERM
长期关注
```

---

# 二十六、ATTENTION 规则

如果只是报告存在异常：

显示：

```text
需要关注
```

不要自动变成：

```text
等待复查
```

---

# 二十七、FOLLOW_UP 规则

只有存在真实：

```text
FollowUpTask
```

才显示：

```text
等待复查
```

FollowUpTask 必须来自：

```text
医生建议
报告明确建议
用户自己设置
系统参考 + 用户确认
```

---

# 二十八、长期关注规则

存在已确认长期疾病时：

一级可显示：

```text
长期关注
```

但不一定直接暴露疾病名称。

进入详情后再显示具体长期关注项目。

---

# 二十九、身体列表状态优先级

如果同一系统存在多个状态：

```text
FOLLOW_UP
>
ATTENTION
>
LONG_TERM
>
RECENT_RECORD
>
NO_RECORD
```

一级列表显示最高优先级。

详情页允许多个状态同时出现。

---

# 三十、身体顶部数字统计

统计必须只基于：

```text
一级器官 / 系统
```

不要把：

```text
电解质
血脂
血糖
代谢指标组
```

单独计入。

---

# 三十一、记录页整体保留

记录页继续保留：

```text
记录
搜索
```

第一层：

```text
全部
报告
影像
手动记录
```

下面是：

```text
时间轴
```

---

# 三十二、记录页筛选重新分层

改成：

第一层：

```text
全部
报告
影像
手动记录
```

第二层：

```text
器官 ▼
筛选
```

第二层视觉权重要低于第一层。

---

# 三十三、记录页高级筛选

“筛选”可继续支持：

```text
时间范围
异常状态
医院
器官 / 系统
是否存在复查
长期关注
```

V1 不需要过度复杂。

---

# 三十四、记录卡片优化

手动记录不要大量使用：

```text
日常记录
```

改用更具体的类型和数值。

例如：

```text
体重
77.6 kg
手动记录
```

血压：

```text
血压
146 / 94 mmHg · 晨起
```

右上角标签优先使用：

```text
报告
影像
血压
血糖
体重
其他
```

---

# 三十五、三个页面职责锁定

## 首页

```text
我现在最需要看到什么
我要怎么快速添加报告
```

## 身体

```text
我的身体哪个器官 / 系统当前是什么状态
```

## 记录

```text
我以前保存过什么健康资料
```

---

# 三十六、慢病继续后台化

继续遵守：

```text
慢病不是一级导航
慢病不是 App 模式
慢病不是首页默认结构
```

内部逻辑：

```text
已确认慢病
↓
关联指标
↓
关联器官 / 系统
↓
长期关注
↓
复查任务
↓
历史趋势
```

---

# 三十七、iOS 交互实现建议

如果项目是 SwiftUI：

优先使用系统：

```text
NavigationStack
ToolbarItem
Menu
Button
TabView
```

不要为了新增菜单继续使用大型自定义 FloatingActionButton。

记录页右上角 `+` 可使用 `ToolbarItem(placement: .topBarTrailing)` + `Menu`。

器官详情页右上角 `+` 同样可以复用同一套 AddMenu，并传入：

```text
contextOrganId
```

---

# 三十八、实施顺序

Claude Code 请严格按以下顺序执行。

## Step 1：先审计

先检查：

```text
Home
Body
Records
TabBar
FloatingAddButton
AddMenu
OrganDetail
NotificationBadge
```

以及：

```text
Attention
FollowUp
Record
Organ
BodySystem
```

相关数据来源。

先输出简短的修改计划，不要直接大改。

## Step 2：删除底部悬浮 +

保留三个 Tab：

```text
首页
身体
记录
```

确保布局正常。

## Step 3：首页保持三个新增框

不要再添加额外 +。

确保“拍报告”仍为第一视觉入口。

## Step 4：记录页右上角增加 +

接入完整新增菜单，复用当前已有新增功能，不要复制一套新的业务逻辑。

## Step 5：器官详情页增加上下文 +

如果当前结构允许，优先复用同一个 AddMenu，允许传 `contextOrganId`。

## Step 6：首页需要关注重构

区分：

```text
HealthAttention
FollowUpTask
ChronicCondition
```

首页不直接高频暴露慢病名称。

## Step 7：通知 Badge 修正

检查 badge 数据来源，减少普通通知进入红色 badge。

## Step 8：身体一级分类统一

只保留：

```text
器官 / 身体系统
```

## Step 9：指标组下沉

将：

```text
电解质
血脂
血糖
肾功能
肝功能
```

等移入系统内部。

## Step 10：ATTENTION / FOLLOW_UP 拆分

确保异常不会自动显示为“等待复查”。

## Step 11：身体统计修正

只统计一级器官 / 系统。

## Step 12：记录筛选重构

第一层：

```text
全部 / 报告 / 影像 / 手动记录
```

第二层：

```text
器官 / 筛选
```

## Step 13：记录卡片文案优化

减少“日常记录”，改用具体类型。

## Step 14：检查导航与交互

重点检查：

```text
首页三个入口
记录页 +
器官详情 +
返回行为
Tab 切换
Menu 弹出
新增完成后的页面刷新
```

## Step 15：验证

运行：

```text
format
lint
test
build
```

如为 Xcode 项目，确保 iOS Simulator Build 通过。

不要为了 build 通过删除业务功能。

---

# 三十九、验收标准

## 全局

- [ ] 底部大型悬浮 `+` 已删除
- [ ] Tab Bar 只保留：首页 / 身体 / 记录
- [ ] UI 更接近原生 iOS 结构
- [ ] 没有因为本次修改大规模重做视觉

## 首页

- [ ] 首页没有额外 `+`
- [ ] 拍报告仍是第一核心入口
- [ ] 相册 / 文件保留
- [ ] 添加影像保留
- [ ] 需要关注区分状态和任务
- [ ] 慢病名称不再默认高频展示
- [ ] 通知 badge 更克制

## 身体

- [ ] 一级只使用器官 / 身体系统
- [ ] 电解质不再作为一级节点
- [ ] 血脂 / 血糖等不再作为一级节点
- [ ] 异常显示需要关注
- [ ] 真实 FollowUpTask 才显示等待复查
- [ ] 暂无记录不生成风险
- [ ] 统计基于一级系统
- [ ] 器官详情允许上下文新增（如当前结构允许）

## 记录

- [ ] 右上角存在轻量 `+`
- [ ] 点击后显示完整新增菜单
- [ ] 首页三个入口之外的辅助添加功能仍可访问
- [ ] 第一层筛选为全部 / 报告 / 影像 / 手动记录
- [ ] 器官 / 筛选是第二层
- [ ] 手动记录卡片不再大量显示“日常记录”
- [ ] 时间轴保持正常

---

# 四十、完成后停止

完成以上任务后停止。

不要继续扩展：

```text
AI
复杂健康评分
体检推荐
疾病预测
社区
商城
资讯
新导航
新主题
```

先检查当前修改结果和页面截图，再决定下一阶段。
